---
description: "React component patterns for Next.js App Router and React 19"
globs: ["**/*.tsx"]
---

# React Component Patterns

## Server vs Client Components

```typescript
// Server Component (default) - no "use client", can be async, no hooks
export default async function Page() {
  const data = await fetchData()
  return <ClientComponent data={data} />
}

// Client Component - "use client" first line, required for hooks
"use client"
export function ClientComponent({ data }: Props) {
  const [state, setState] = useState(data)
  return <div>{state}</div>
}
```

## Hook Patterns

```typescript
// useCallback for stable function refs
const handleSubmit = useCallback((data: FormData) => {
  // implementation
}, [dependency])

// useMemo for expensive computations
const sorted = useMemo(
  () => [...items].sort((a, b) => a.date - b.date),
  [items]
)

// Return typed objects from hooks
export function useChatCore() {
  return useMemo(() => ({
    messages, sendMessage, isLoading,
  }), [messages, sendMessage, isLoading])
}
```

## Component Props

```typescript
export type ChatMessageProps = {
  message: Message
  onEdit: (id: string, content: string) => void
}

export function ChatMessage({ message, onEdit }: ChatMessageProps) {
  // ...
}
```
