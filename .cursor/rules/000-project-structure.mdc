---
description: "Core project structure and file organization for Not A Wrapper"
---

# Project Structure

## Directory Layout

```
app/                    # Next.js App Router
├── api/               # API routes (streaming, REST)
├── auth/              # Auth pages (Clerk)
├── components/        # App-specific components
│   ├── chat/         # Chat UI (gold standard: chat.tsx)
│   ├── chat-input/   # Input components
│   └── layout/       # App layout
├── hooks/            # App-specific hooks
└── types/            # App-specific types

lib/                   # Shared utilities
├── chat-store/       # Chat state (gold standard: chats/provider.tsx)
├── models/           # AI model definitions
├── openproviders/    # AI provider abstraction
└── config.ts         # Centralized constants

components/           # Shadcn UI components (shared)
convex/              # Database schema & functions

.agents/              # AI context & knowledge base
├── context/         # Domain knowledge & references
├── research/        # Research & evaluations
├── troubleshooting/ # Known issues & fixes
├── design/          # Design references & UI research
├── plans/           # Implementation plans
├── skills/          # Multi-step task guides
├── workflows/       # Development procedures
└── archive/         # Superseded documents
```

## Where to Add Code

| Type | Location |
|------|----------|
| New page | `app/[route]/page.tsx` |
| API route | `app/api/[route]/route.ts` |
| Chat component | `app/components/chat/` |
| Shared UI | `components/ui/` |
| State provider | `lib/[feature]-store/provider.tsx` |
| Utility function | `lib/utils.ts` or `lib/[feature].ts` |
| Database function | `convex/[table].ts` |
| Type definition | Colocate with feature or `app/types/` |

## Where to Add Documentation

| Type | Location |
|------|----------|
| Research / evaluation | `.agents/research/` |
| Known issue / fix | `.agents/troubleshooting/` |
| Design reference / UI research | `.agents/design/` |
| Domain reference | `.agents/context/` |
| Implementation plan | `.agents/plans/` |
| New skill guide | `.agents/skills/[name]/SKILL.md` |
| Workflow / procedure | `.agents/workflows/` |
| Superseded document | `.agents/archive/` |
| Session scratch notes | `NOTES.md` (project root) |

> **Rule**: Never create loose markdown files in the project root. All documentation belongs in `.agents/` unless it is `README.md`, `AGENTS.md`, `CLAUDE.md`, `NOTES.md`, or `INSTALL.md`.

## Gold Standard Examples

- **API Route**: `app/api/chat/route.ts`
- **Custom Hook**: `app/components/chat/use-chat-core.ts`
- **Context Provider**: `lib/chat-store/chats/provider.tsx`
- **Component**: `app/components/chat/chat.tsx`
